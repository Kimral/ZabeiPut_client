import 'package:flutter/material.dart';

class CPasswordRecovery extends StatelessWidget {
  const CPasswordRecovery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
          appBar: AppBar(
            title: Text("Восстановление пароля"),
          ),
          body: Container(

          )
      ),
    );
  }
}
